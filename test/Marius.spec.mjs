import 'mocha'
import {
    threwError,
    did,
    have,
    is,
    matches,
    getCounter,
    count,
    valueMatch,
    throwsError,
    nullCheck
} from '../libs/ChaiFunctions/ChaiFunctions.mjs'
import {
    running
} from '../Marius.mjs'


describe('Crops', () => {
    describe('Constructor', () => {
        running(true)
    })
})
